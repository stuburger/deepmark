node_modules
build

# sst
.sst

# env
.env
.env.*

.turbo
dist

packages/db/src/generated/
.open-next
biome-audit.txt

# Local sample scripts pulled from S3 for testing — never committed.
# Contains real student handwriting + names; re-download from S3 if needed.
sample-data/

# Local scratch dir for pulling artefacts (PDFs, traces) out of S3/DB.
tmp/

# Real-student stress fixture for batch-classify smoke test (44 MB,
# contains real Year 10 mock answers + names from production).
# Re-fetch from production S3 if needed:
#   s3://deepmark-production-scansbucketbucket-oxttmuus/batches/cmorezqfn000302lb6v5rkn62/source/GWAUGH Exams.pdf
y10_papers/gwaugh-700-page.pdf
